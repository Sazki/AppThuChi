<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/bg1"/>   <!-- Nền -->
    <stroke android:width="2dp" android:color="@color/bg1"/>  <!-- Viền -->
    <corners android:radius="20dp"/>   <!-- Bo góc -->
</shape>